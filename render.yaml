services:
  - type: web
    name: bdms-web
    env: docker
    repo: https://github.com/youruser/yourrepo
    branch: main
    dockerfilePath: Dockerfile
    plan: free
    envVars:
      - key: APP_ENV
        value: production
      - key: DB_HOST
        sync: false